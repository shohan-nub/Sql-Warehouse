import { newPurchase } from "@/allType";



export class PurchaseServices{
    private purchaseRepo =new PurchaseServices();

    async createPurchase(body:newPurchase){
        const purchaseRepo=this.purchaseRepo.

    }
}